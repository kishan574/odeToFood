@*
    use of view imports is used to render the necessary namespace's needed for the data models.
    this reduces the need to call the namespace in every single pages.
*@

@using ode2Food.Models;
@using ode2Food.ViewModels;

@addTagHelper *,Microsoft.AspNetCore.Mvc.TagHelpers
